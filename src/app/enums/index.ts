export enum EIcon {
  arrowBack = 'arrowBack',
  search = 'search',
  share = 'share',
  arrowRight = 'arrowRight',
  google = 'google',
  facebook = 'facebook',
  star = 'star',
  favorite = 'favorite',
  home = 'home',
  shop = 'shop',
  bag = 'bag',
  profile = 'profile',
}

export enum EButton {
  primary = 'primary',
  text = 'text',
}

export enum EJustifyContent {
  'flex-start' = 'flex-start',
  'center' = 'center',
  'flex-end' = 'flex-end',
}
