export enum EIcon {
  arrowBack = 'arrowBack',
  search = 'search',
  share = 'share',
  arrowRight = 'arrowRight',
  google = 'google',
  facebook = 'facebook',
}

export enum EButton {
  primary = 'primary',
  text = 'text',
}

export enum EJustifyContent {
  'flex-start' = 'flex-start',
  'center' = 'center',
  'flex-end' = 'flex-end',
}
